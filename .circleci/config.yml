version: 2.1

orbs:
  rok8s: fairwinds/rok8s-scripts@16.0.0
  slack: circleci/slack@6.1.3

commands:
  set_vault:
    description: Install and configure vault
    steps:
      - run: ./.circleci/scripts/vault.sh
      - rok8s/get_vault_env:
          vault_path: repo/global/env

references:
  setup_environment: &setup_environment
    run:
      name: Setup Environment
      command: |
        echo 'export CI_SHA1=$CIRCLE_SHA1' >> ${BASH_ENV}
        echo 'Install AWS CLI'
        apk --no-cache add aws-cli
        echo 'Install needed packages'
        apk --no-cache add bash jq curl yq

  check_changelog: &check_changelog
    run:
      name: Check CHANGELOG
      command: |
        require_changelog=( insights-agent fairwinds-insights insights-admission )
        for chart in "${require_changelog[@]}"; do
          version=$(yq eval '.version' ./stable/$chart/Chart.yaml )
          echo "Checking if CHANGELOG.md is updated for $chart"
          grep "## $version" ./stable/$chart/CHANGELOG.md
        done

  install_vault_alpine: &install_vault_alpine
    run:
      name: install hashicorp vault
      command: |
        apk --update add curl yq
        cd /tmp
        curl -LO https://releases.hashicorp.com/vault/1.13.2/vault_1.13.2_linux_amd64.zip
        sha256sum vault_1.13.2_linux_amd64.zip | grep f7930279de8381de7c532164b4a4408895d9606c0d24e2e9d2f9acb5dfe99b3c
        unzip vault_1.13.2_linux_amd64.zip
        mv vault /usr/bin/vault

  kind_configuration_helm3: &kind_configuration_helm3
    pre_script: scripts/circle-pre-e2e.sh
    script: scripts/circle-e2e.sh
    command_runner_image: quay.io/helmpack/chart-testing:v3.14.0
    requires:
      - lint-scripts
      - lint-charts
      - check-helm-docs
    filters:
      branches:
        only: /.*/
      tags:
        ignore: /.*/

executors:
  ci-images:
    docker:
      - image: quay.io/reactiveops/ci-images:v15-cimg-24.04
  ci-images-xlarge:
    resource_class: xlarge
    docker:
      - image: quay.io/reactiveops/ci-images:v15-cimg-24.04

jobs:
  # On Renovate PRs, update CHANGELOG.md and regenerate helm-docs READMEs, then push to the PR branch.
  bump_renovate_pr_changelog:
    executor: ci-images
    steps:
      - checkout
      - set_vault
      - run:
          name: Bump changelog and helm-docs for Renovate PR
          command: ./.circleci/scripts/bump-renovate-pr.sh

  check-helm-docs:
    executor: ci-images
    steps:
      - checkout
      - run:
          name: Install helm-docs
          command: |
            HELM_DOCS_VERSION=1.14.2
            cd /tmp
            curl -LO "https://github.com/norwoodj/helm-docs/releases/download/v${HELM_DOCS_VERSION}/helm-docs_${HELM_DOCS_VERSION}_Linux_x86_64.tar.gz"
            tar -zxvf "helm-docs_${HELM_DOCS_VERSION}_Linux_x86_64.tar.gz"
            mv helm-docs /usr/local/bin/helm-docs
            chmod +x /usr/local/bin/helm-docs
      - run:
          name: Check Docs for Changes
          command: |
            helm-docs --sort-values-order=file
            git diff --exit-code
      - *check_changelog

  lint-scripts:
    docker:
      - image: koalaman/shellcheck-alpine:v0.11.0
    steps:
      - checkout
      - run:
          name: lint
          command: |
            for script in $(ls scripts/*.sh); do
                shellcheck -x $script
            done
      - run:
          name: lint-pre-scripts
          command: |
            for script in $(find incubator stable -type f -name pre-test-script.sh); do
                shellcheck -x $script
            done

  lint-charts:
    docker:
      - image: quay.io/helmpack/chart-testing:v3.14.0
    steps:
      - checkout
      - *setup_environment
      - run:
          name: lint
          command: |
            git remote add ro https://github.com/fairwindsops/charts
            git fetch ro master
            ct lint --config scripts/ct.yaml --chart-yaml-schema scripts/schema.yaml --debug --print-config

  sync:
    docker:
      - image: quay.io/helmpack/chart-testing:v3.14.0
    steps:
      - checkout
      - *setup_environment
      - *install_vault_alpine
      - rok8s/get_vault_aws_credentials:
          vault_path: fairwindsops/sts/charts-docs
      - rok8s/get_vault_env:
          vault_path: repo/charts/env
      - run:
          name: sync
          command: scripts/sync.sh

  # TEMP INS-2537: delete force-fail-slack-alert-test + branch allowlists before merge
  force-fail-slack-alert-test:
    docker:
      - image: cimg/base:current
    steps:
      - run:
          name: Force fail to exercise Slack alert
          command: |
            echo "TEMP INS-2537: forcing failure to test alert-master-release-failure"
            exit 1

  alert-master-release-failure:
    executor: ci-images
    steps:
      - checkout
      - set_vault
      - run:
          name: Ensure Slack default channel
          command: |
            # pod-dev-notifications — same default as insights-release
            echo 'export SLACK_DEFAULT_CHANNEL="${SLACK_DEFAULT_CHANNEL:-CPYBX810T}"' >> "${BASH_ENV}"
      - slack/notify:
          event: always
          # TEMP INS-2537: remove feature branch from branch_pattern before merge
          branch_pattern: master,monitoring-charts-release/vv/INS-2537
          template: basic_fail_1

workflows:
  version: 2
  test-and-sync:
    jobs:
      - bump_renovate_pr_changelog:
          filters:
            branches:
              only: /renovate\/.*/
      - check-helm-docs
      - lint-scripts
      - lint-charts
      # TEMP INS-2537: delete before merge
      - force-fail-slack-alert-test:
          filters:
            branches:
              only:
                - monitoring-charts-release/vv/INS-2537
      - rok8s/kubernetes_e2e_tests:
          name: "End-To-End Kubernetes 1.33"
          kind_node_image: "kindest/node:v1.33.12@sha256:3f5c8443c620245e4d355cfe09e96a91ead32ceaa569d3f1ca9edf0cb2fe2ff4"
          checkout-method: full
          executor:
            name: ci-images-xlarge
          <<: *kind_configuration_helm3
      - rok8s/kubernetes_e2e_tests:
          name: "End-To-End Kubernetes 1.34"
          kind_node_image: "kindest/node:v1.34.8@sha256:02722c2dedddcfc00febf5d27fbeb9b7b2c14294c82109ff4a85d89ac9ba3256"
          checkout-method: full
          executor:
            name: ci-images-xlarge
          <<: *kind_configuration_helm3
      - rok8s/kubernetes_e2e_tests:
          name: "End-To-End Kubernetes 1.35"
          kind_node_image: "kindest/node:v1.35.5@sha256:ce977ae6d65918d0b58a5f8b5e940429c2ce42fa3a5619ec2bbc60b949c0ac95"
          checkout-method: full
          executor:
            name: ci-images-xlarge
          <<: *kind_configuration_helm3
      - rok8s/kubernetes_e2e_tests:
          name: "End-To-End Kubernetes 1.36"
          kind_node_image: "kindest/node:v1.36.1@sha256:3489c7674813ba5d8b1a9977baea8a6e553784dab7b84759d1014dbd78f7ebd5"
          checkout-method: full
          executor:
            name: ci-images-xlarge
          <<: *kind_configuration_helm3
      - sync:
          requires:
            - lint-scripts
            - lint-charts
            - check-helm-docs
          filters:
            branches:
              only:
                - master
      - alert-master-release-failure:
          requires:
            - lint-scripts
            - lint-charts
            - check-helm-docs
            - sync
            # TEMP INS-2537: delete before merge
            - force-fail-slack-alert-test
          when: on_fail
          filters:
            branches:
              only:
                - master
                # TEMP INS-2537: delete before merge
                - monitoring-charts-release/vv/INS-2537
