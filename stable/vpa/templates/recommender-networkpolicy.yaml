{{- if and .Values.recommender.enabled .Values.recommender.networkPolicy.enabled }}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "vpa.fullname" . }}-recommender
  labels:
    app.kubernetes.io/component: recommender
    {{- include "vpa.labels" . | nindent 4 }}
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/component: recommender
      {{- include "vpa.selectorLabels" . | nindent 6 }}
  {{- if or .Values.recommender.networkPolicy.ingress .Values.recommender.networkPolicy.egress }}
  policyTypes:
    {{- if .Values.recommender.networkPolicy.ingress }}
    - Ingress
    {{- end }}
    {{- if .Values.recommender.networkPolicy.egress }}
    - Egress
    {{- end }}
  {{- end }}
  {{- if .Values.recommender.networkPolicy.ingress }}
  ingress:
    {{- toYaml .Values.recommender.networkPolicy.ingress | nindent 4 }}
  {{- end }}
  {{- if .Values.recommender.networkPolicy.egress }}
  egress:
    {{- toYaml .Values.recommender.networkPolicy.egress | nindent 4 }}
  {{- end }}
{{- end }}
