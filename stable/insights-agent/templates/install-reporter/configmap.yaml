apiVersion: v1
kind: ConfigMap
metadata:
  {{- include "metadata" "values" }}
  name: {{ include "insights-agent.fullname" $ }}-values
data:
  values.json: |
    {{ $values := .Values | deepCopy }}
    {{ $_ := unset $values "insights" }}
    {{- toPrettyJson $values | nindent 4 }}
