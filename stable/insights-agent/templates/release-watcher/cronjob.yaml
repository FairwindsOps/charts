{{- if .Values.release-watcher.enabled -}} 
{{- $_ := set . "Label" "release-watcher" }}
{{- $_ := set . "Config" .Values.release-watcher }}
{{- $_ := set . "Template" "release-watcher.job.spec" }}
{{- include "cronjob" . }}
{{- end -}}
