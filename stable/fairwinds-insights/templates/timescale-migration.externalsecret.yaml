{{- $externalSecret := .Values.timescale.auth.migrationExternalSecret | default dict }}
{{- if $externalSecret.create }}
{{- include "fairwinds-insights.validateTimescaleAuth" . }}
{{- if not $externalSecret.data }}
{{- fail "timescale.auth.migrationExternalSecret.create is true but timescale.auth.migrationExternalSecret.data is empty; provide at least one entry with secretKey and remoteRef.key (property optional)." }}
{{- end }}
{{- range $i, $entry := $externalSecret.data }}
{{- if not $entry.secretKey }}
{{- fail (printf "timescale.auth.migrationExternalSecret.data[%d] is missing required field 'secretKey'" $i) }}
{{- end }}
{{- if not $entry.remoteRef }}
{{- fail (printf "timescale.auth.migrationExternalSecret.data[%d] is missing required field 'remoteRef'" $i) }}
{{- end }}
{{- if not $entry.remoteRef.key }}
{{- fail (printf "timescale.auth.migrationExternalSecret.data[%d].remoteRef is missing required field 'key'" $i) }}
{{- end }}
{{- end }}
{{- $secretName := include "fairwinds-insights.timescaleMigrationSecret" . }}
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: {{ include "fairwinds-insights.timescaleMigrationExternalSecretName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "fairwinds-insights.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": "pre-install,pre-upgrade"
    "helm.sh/hook-weight": "-10"
  {{- with $externalSecret.annotations }}
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  refreshPolicy: Periodic
  refreshInterval: {{ $externalSecret.refreshInterval | default "1h" }}
  secretStoreRef:
    name: {{ $externalSecret.secretStoreRef.name }}
    kind: {{ $externalSecret.secretStoreRef.kind }}
  target:
    name: {{ $secretName }}
    creationPolicy: Owner
    template:
      type: Opaque
  data:
    {{- toYaml $externalSecret.data | nindent 4 }}
{{- end }}
