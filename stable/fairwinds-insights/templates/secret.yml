{{- include "fairwinds-insights.validateDatabaseSecrets" . }}
{{- if .Values.options.autogenerateKeys }}
apiVersion: v1
data:
    COOKIE_HASH_KEY: {{ randAlphaNum 64 | b64enc }}
    COOKIE_BLOCK_KEY: {{ randAlphaNum 32 | b64enc }}
    SESSION_AUTH_KEY: {{ randAlphaNum 64 | b64enc }}
    SESSION_ENCRYPTION_KEY: {{ randAlphaNum 32 | b64enc }}
    AES_BASE64_CYPHER_KEY: {{ default (randAlphaNum 32) .Values.encryption.aes.cypherKey | b64enc }}
kind: Secret
metadata:
    name: {{ .Values.options.secretName }}
type: Opaque
{{- end }}
{{- if eq (include "fairwinds-insights.postgresqlManageAppSecret" .) "true" }}
---
{{- $password := include "fairwinds-insights.ephemeralSecretPassword" (dict "root" . "value" .Values.postgresql.auth.password "secretName" (include "fairwinds-insights.postgresqlSecretName" .) "key" "password") }}
apiVersion: v1
data:
    password: {{ b64enc $password }}
    username: {{ b64enc (include "fairwinds-insights.postgresqlAppUsername" .) }}
kind: Secret
metadata:
  name: {{ include "fairwinds-insights.postgresqlSecretName" . }}
  annotations:
    "helm.sh/hook": "pre-install,pre-upgrade"
    "helm.sh/hook-weight": "-1"
    "helm.sh/resource-policy": keep
type: Opaque
{{- end }}
{{- if eq (include "fairwinds-insights.postgresqlManageMigrationSecret" .) "true" }}
---
{{- $migrationPassword := .Values.postgresql.auth.migrationPassword }}
{{- if not $migrationPassword }}
{{- $migrationPassword = include "fairwinds-insights.ephemeralSecretPassword" (dict "root" . "value" "" "secretName" (include "fairwinds-insights.postgresqlMigrationSecret" .) "key" "password") }}
{{- end }}
apiVersion: v1
data:
    password: {{ b64enc $migrationPassword }}
    username: {{ b64enc (include "fairwinds-insights.postgresqlMigrationUsername" .) }}
kind: Secret
metadata:
  name: {{ include "fairwinds-insights.postgresqlMigrationSecret" . }}
  annotations:
    "helm.sh/hook": "pre-install,pre-upgrade"
    "helm.sh/hook-weight": "-1"
    "helm.sh/resource-policy": keep
type: Opaque
{{- end }}
{{- if eq (include "fairwinds-insights.postgresqlManageSuperUserSecret" .) "true" }}
---
apiVersion: v1
data:
    username: {{ b64enc "postgres" }}
    password: {{ b64enc (include "fairwinds-insights.ephemeralSecretPassword" (dict "root" . "value" .Values.postgresql.auth.superuserpassword "secretName" (include "fairwinds-insights.postgresqlSuperUserSecretName" .) "key" "password")) }}
kind: Secret
metadata:
  name: {{ include "fairwinds-insights.postgresqlSuperUserSecretName" . }}
  annotations:
    "helm.sh/hook": "pre-install,pre-upgrade"
    "helm.sh/hook-weight": "-1"
    "helm.sh/resource-policy": keep
type: Opaque
{{- end }}
{{- if eq (include "fairwinds-insights.timescaleManageAppSecret" .) "true" }}
---
{{- $tsPassword := include "fairwinds-insights.ephemeralSecretPassword" (dict "root" . "value" .Values.timescale.auth.password "secretName" (include "fairwinds-insights.timescaleSecretName" .) "key" "password") }}
apiVersion: v1
data:
    password: {{ b64enc $tsPassword }}
    username: {{ b64enc (include "fairwinds-insights.timescaleAppUsername" .) }}
kind: Secret
metadata:
  name: {{ include "fairwinds-insights.timescaleSecretName" . }}
  annotations:
    "helm.sh/hook": "pre-install,pre-upgrade"
    "helm.sh/hook-weight": "-1"
    "helm.sh/resource-policy": keep
type: Opaque
{{- end }}
{{- if eq (include "fairwinds-insights.timescaleManageMigrationSecret" .) "true" }}
---
{{- $tsMigrationPassword := .Values.timescale.auth.migrationPassword }}
{{- if not $tsMigrationPassword }}
{{- $tsMigrationPassword = include "fairwinds-insights.ephemeralSecretPassword" (dict "root" . "value" "" "secretName" (include "fairwinds-insights.timescaleMigrationSecret" .) "key" "password") }}
{{- end }}
apiVersion: v1
data:
    password: {{ b64enc $tsMigrationPassword }}
    username: {{ b64enc (include "fairwinds-insights.timescaleMigrationUsername" .) }}
kind: Secret
metadata:
  name: {{ include "fairwinds-insights.timescaleMigrationSecret" . }}
  annotations:
    "helm.sh/hook": "pre-install,pre-upgrade"
    "helm.sh/hook-weight": "-1"
    "helm.sh/resource-policy": keep
type: Opaque
{{- end }}
{{- if eq (include "fairwinds-insights.timescaleManageSuperUserSecret" .) "true" }}
---
apiVersion: v1
data:
    username: {{ b64enc "postgres" }}
    password: {{ b64enc (include "fairwinds-insights.ephemeralSecretPassword" (dict "root" . "value" .Values.timescale.auth.superuserpassword "secretName" (include "fairwinds-insights.timescaleSuperUserSecretName" .) "key" "password")) }}
kind: Secret
metadata:
  name: {{ include "fairwinds-insights.timescaleSuperUserSecretName" . }}
  annotations:
    "helm.sh/hook": "pre-install,pre-upgrade"
    "helm.sh/hook-weight": "-1"
    "helm.sh/resource-policy": keep
type: Opaque
{{- end }}
{{- if .Values.timescale.ephemeral }}
---
{{- $timescaleCASecretName := "fwinsights-timescale-ca" }}
{{- $existingTimescaleCA := lookup "v1" "Secret" .Release.Namespace $timescaleCASecretName }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ $timescaleCASecretName }}
  annotations:
    "helm.sh/hook": "pre-install,pre-upgrade"
    "helm.sh/hook-weight": "-1"
type: kubernetes.io/tls
{{- if and $existingTimescaleCA $existingTimescaleCA.data (index $existingTimescaleCA.data "tls.crt") (index $existingTimescaleCA.data "tls.key") }}
data:
  tls.crt: {{ index $existingTimescaleCA.data "tls.crt" }}
  tls.key: {{ index $existingTimescaleCA.data "tls.key" }}
{{- else }}
{{ $ca := genCA "timescale" 1826 -}}
stringData:
    tls.crt: {{ $ca.Cert | quote }}
    tls.key: {{ $ca.Key  | quote }}
{{- end }}
{{- end }}
{{- if .Values.postgresql.ephemeral }}
---
{{- $postgresqlCASecretName := "fwinsights-postgresql-ca" }}
{{- $existingPostgresqlCA := lookup "v1" "Secret" .Release.Namespace $postgresqlCASecretName }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ $postgresqlCASecretName }}
  annotations:
    "helm.sh/hook": "pre-install,pre-upgrade"
    "helm.sh/hook-weight": "-1"
type: kubernetes.io/tls
{{- if and $existingPostgresqlCA $existingPostgresqlCA.data (index $existingPostgresqlCA.data "tls.crt") (index $existingPostgresqlCA.data "tls.key") }}
data:
  tls.crt: {{ index $existingPostgresqlCA.data "tls.crt" }}
  tls.key: {{ index $existingPostgresqlCA.data "tls.key" }}
{{- else }}
{{ $ca := genCA "postgres" 1826 -}}
stringData:
    tls.crt: {{ $ca.Cert | quote }}
    tls.key: {{ $ca.Key  | quote }}
{{- end }}
{{- end }}
