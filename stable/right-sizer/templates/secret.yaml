{{- if .Values.createSecret }}
apiVersion: v1
data:
  INSIGHTS_TOKEN: {{ required "You must set insights.base64token" .Values.insights.base64token | quote }}
kind: Secret
metadata:
  name: {{ include "right-sizer.fullname" $ }}
  {{- if $.Values.templateOnly }}
  namespace: {{ $.Release.Namespace }}
  {{- end }}
  labels:
    app.kubernetes.io/name: {{ include "right-sizer.name" $ }}
    helm.sh/chart: {{ include "right-sizer.chart" $ }}
    app.kubernetes.io/instance: {{ $.Release.Name }}
    app.kubernetes.io/managed-by: {{ $.Release.Service }}
    app.kubernetes.io/component: controller
type: Opaque
{{- end }}
