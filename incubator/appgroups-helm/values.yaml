# -- The list of dev appGroups
devAppGroups:
  # -- dev-one
  - name: dev-one
    labels:
      - "foo": "bar"
      - "key": "value"
    namespaceLabels:
      - "fargle": "bargle"
  # -- dev-two
  - name: dev-two
# -- The list of prod appGroups
prodAppGroups:
  - name: prod-one

# -- labels to use when selecting production stuff
prodLabels:
  - "env": "prod"


devClusters:
  - us-east1-dev*
  - us-east2-dev*
testClusters:
  - us-east1-test*
  - us-east2-test*
prodClusters:
  - us-east1-prod*
  - us-east2-prod*
