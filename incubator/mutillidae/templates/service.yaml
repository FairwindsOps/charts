apiVersion: v1
kind: Service
metadata:
  name: {{ include "mutillidae.fullname" . }}
  labels:
    {{- include "mutillidae.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mutillidae.selectorLabels" . | nindent 4 }}
