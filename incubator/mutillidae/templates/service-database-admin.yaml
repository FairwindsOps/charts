apiVersion: v1
kind: Service
metadata:
  name: {{ include "mutillidae.fullname" . }}-database-admin
  labels:
    {{- include "mutillidae.labels" . | nindent 4 }}
    app.kubernetes.io/component: database-admin
spec:
  type: {{ .Values.databaseAdmin.service.type }}
  ports:
    - port: {{ .Values.databaseAdmin.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mutillidae.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: database-admin
