{{- if .Values.databaseAdmin.ingress.enabled -}}
{{- $fullName := include "mutillidae.fullname" . -}}
{{- $svcPort := .Values.databaseAdmin.service.port -}}
{{- if and .Values.databaseAdmin.ingress.className (not (semverCompare ">=1.18-0" .Capabilities.KubeVersion.GitVersion)) }}
  {{- if not (hasKey .Values.databaseAdmin.ingress.annotations "kubernetes.io/ingress.class") }}
  {{- $_ := set .Values.databaseAdmin.ingress.annotations "kubernetes.io/ingress.class" .Values.databaseAdmin.ingress.className}}
  {{- end }}
{{- end }}
{{- if semverCompare ">=1.19-0" .Capabilities.KubeVersion.GitVersion -}}
apiVersion: networking.k8s.io/v1
{{- else if semverCompare ">=1.14-0" .Capabilities.KubeVersion.GitVersion -}}
apiVersion: networking.k8s.io/v1beta1
{{- else -}}
apiVersion: extensions/v1beta1
{{- end }}
kind: Ingress
metadata:
  name: {{ $fullName }}-database-admin
  labels:
    {{- include "mutillidae.labels" . | nindent 4 }}
    app.kubernetes.io/component: database-admin
  {{- with .Values.databaseAdmin.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if and .Values.databaseAdmin.ingress.className (semverCompare ">=1.18-0" .Capabilities.KubeVersion.GitVersion) }}
  ingressClassName: {{ .Values.databaseAdmin.ingress.className }}
  {{- end }}
  {{- if .Values.databaseAdmin.ingress.tls }}
  tls:
    {{- range .Values.databaseAdmin.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.databaseAdmin.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            {{- if and .pathType (semverCompare ">=1.18-0" $.Capabilities.KubeVersion.GitVersion) }}
            pathType: {{ .pathType }}
            {{- end }}
            backend:
              {{- if semverCompare ">=1.19-0" $.Capabilities.KubeVersion.GitVersion }}
              service:
                name: {{ $fullName }}-database-admin
                port:
                  number: {{ $svcPort }}
              {{- else }}
              serviceName: {{ $fullName }}-database-admin
              servicePort: {{ $svcPort }}
              {{- end }}
          {{- end }}
    {{- end }}
{{- end }}
