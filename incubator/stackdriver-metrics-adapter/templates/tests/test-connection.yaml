apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "stackdriver-metrics-adapter.fullname" . }}-test-connection"
  labels:
{{ include "stackdriver-metrics-adapter.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "stackdriver-metrics-adapter.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
