apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "insights-admission.fullname" . }}-test-connection"
  labels:
    {{- include "insights-admission.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "insights-admission.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
